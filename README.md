# Multi-Model-Article-Classification
Uses different models (ML, DL, Transformers) Allows users to input text Shows predicted category + confidence Runs as a web app using Streamlit Is deployed on AWS cloud
